spring:
    application:
        name: zuul
    cloud:
        config:
            enabled: false
eureka:
    client:
        serviceUrl:
            defaultZone: http://discovery:8761/eureka/
security:
  basic:
    enabled: false